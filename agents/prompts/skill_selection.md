# Skill Selection Prompt

Use Skill Registry metadata to choose a skill. Prefer explicit task intent, available input types, risk level, review requirement, and declared next skills.

If inputs are incomplete, return missing inputs instead of running a workflow.
