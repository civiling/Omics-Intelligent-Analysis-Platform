# Method Notes

The workflow inspects delimited expression matrices without loading full dense matrices into memory. It records dimensions, orientation, raw count compatibility, organism hints, and gene identifier type.
